<!-- brainclaw:start -->
# shared_agent_memory_mvp

> This file is generated by brainclaw. Run `brainclaw export --format claude-md --write` to regenerate.

## Project Instructions

- Après chaque merge dans master : mettre à jour les fichiers docs/ concernés par les changements — docs/cli.md si nouvelles commandes ou options, docs/mcp-schema-changelog.md si les outils MCP ont changé, docs/context-format-changelog.md si le format de contexte a évolué, docs/concepts/ si les concepts ou schémas ont changé. Puis régénérer CLAUDE.md via : brainclaw export --format claude-md --write

## Active Constraints

- Windows : node absent du PATH dans VS Code. Utiliser le chemin complet: & 'C:\Program Files\nodejs\node.exe'. Alternative : ajouter au PATH via $env:Path = 'C:\Program Files\nodejs;' + C:\Program Files\PowerShell\7;c:\Users\jberdah\AppData\Roaming\Code\User\globalStorage\github.copilot-chat\debugCommand;c:\Users\jberdah\AppData\Roaming\Code\User\globalStorage\github.copilot-chat\copilotCli;C:\Users\jberdah\AppData\Local\Programs\Microsoft VS Code;C:\Program Files (x86)\Common Files\Oracle\Java\java8path;C:\Program Files (x86)\Common Files\Oracle\Java\javapath;C:\WINDOWS;C:\WINDOWS\system32;C:\WINDOWS\System32\Wbem;C:\WINDOWS\System32\WindowsPowerShell\v1.0\;C:\WINDOWS\System32\OpenSSH\;C:\Python311\Scripts\;C:\Python311\;C:\Program Files\PuTTY\;C:\Program Files (x86)\Bitvise SSH Client;C:\Program Files\Git\cmd;C:\ProgramData\chocolatey\bin;C:\Program Files (x86)\NVIDIA Corporation\PhysX\Common;C:\Program Files\dotnet\;C:\WINDOWS\system32;C:\WINDOWS;C:\WINDOWS\System32\Wbem;C:\Program Files\PowerShell\7\;C:\Program Files\Apache\apache-maven-3.9.12\bin;;C:\Program Files\Docker\Docker\resources\bin;C:\Program Files\nodejs\;C:\Users\jberdah\AppData\Local\Programs\Python\Launcher\;C:\Users\jberdah\scoop\shims;C:\Users\jberdah\AppData\Local\Microsoft\WindowsApps;C:\Users\jberdah\AppData\Local\Programs\Microsoft VS Code\bin;C:\Users\jberdah\AppData\Local\Google\Cloud SDK\google-cloud-sdk\bin;C:\Users\jberdah\AppData\Local\Microsoft\WindowsApps;C:\Users\jberdah\.dotnet\tools;C:\Program Files (x86)\Java\jre1.8.0_441\bin;C:\Users\jberdah\AppData\Local\Programs\Antigravity\bin;C:\Users\jberdah\bin;C:\Users\jberdah\bin;C:\Users\jberdah\AppData\Local\Programs\Ollama;C:\Users\jberdah\AppData\Roaming\npm.
- TypeScript module: Node16 + ESM — les imports locaux doivent avoir l'extension .js (pas .ts). Le build output va dans dist/ (prod) et dist-test/ (tests). tsconfig.test.json est distinct de tsconfig.json.
- Coverage gates c8: lines 55%, functions 60%, branches 65%, statements 55%. Ne pas baisser ces seuils. Objectif long terme: hausser chaque milestone vers 80/85/80/80.
- Dépendances prod: commander, yaml, zod SEULEMENT. Ne pas ajouter de dépendances runtime sans discussion explicite — le projet est volontairement minimaliste pour permettre des agents légers.
- Toujours bumper la version dans package.json après un merge dans master : modifier la version selon semver (patch/minor/major), rebuilder le dist avec tsc, et publier via brainclaw version --publish-local avec une release note décrivant les changements.

## Known Traps

- [medium] mcp.ts : un objet literal ne peut pas avoir deux propriétés avec le même nom (TS1117). Lors de la refactorisation des handlers MCP, vérifier les objets construits pour des props dupliquées — typiquement tags ou text qui apparaissent une fois avec l'ancienne expression et une fois avec la nouvelle variable.
- [medium] TTL silently ignored dans trap.ts et runtime-note.ts : avant 0.5.4, un TTL invalide était converti en undefined et passé sans erreur. Tout nouveau handler qui parse un TTL doit appeler validateCliTtl() AVANT d'utiliser la valeur, pas après.
- [medium] PowerShell : la syntaxe 'node.exe' path 2>&1 | Select fail avec 'Unexpected token'. Utiliser & devant le chemin entre guillemets : & 'C:\Program Files\nodejs\node.exe' args. Le pipe fonctionne ensuite normalement.
- [medium] Les tests brainclaw (node --test) doivent tourner sur les fichiers dist-test/*.js, pas les .ts sources. Après chaque changement, toujours rebuilder avec tsc -p tsconfig.test.json avant de lancer node --test.
- [medium] Git push SSH ne fonctionne pas depuis PowerShell sur ce poste : utiliser 'wsl -- bash -c ...' ou le terminal WSL directement.
- [medium] WSL peut etre trompeur sur ce poste: avant installation native, npm pouvait repondre alors que node etait absent, et lancer 'bash node_modules/typescript/bin/tsc' est invalide. Verifier explicitement 'node -v' et utiliser 'node node_modules/typescript/bin/tsc'.
- [medium] WSL peut etre trompeur sur ce poste: avant installation native, npm pouvait repondre alors que node etait absent, et lancer 'bash node_modules/typescript/bin/tsc' est invalide. Verifier explicitement 'node -v' et utiliser 'node node_modules/typescript/bin/tsc'.
- [high] Sous WSL natif, ne pas conclure trop vite a un bug MCP quand le message est 'Project memory not initialized'. Sur ce poste, le harness Node avec spawnSync/pipe peut laisser init incomplet ou non fiable; verifier d'abord une repro shell directe 'init' puis 'mcp'.
- [medium] Sur ce poste, les commandes manuelles cmd/PowerShell avec redirections ou sorties tronquees peuvent faire croire a un echec du subset cible alors que la tache Windows dediee targeted-test-validation-shortpath passe a 105/105. Preferer la tache du workspace ou un log capture propre pour diagnostiquer.
- [medium] Windows local release publishing is unreliable when invoking npm directly from Node child_process. For brainclaw version --publish-local, use cmd /c npm pack on Windows instead of spawnSync('npm') or npm.cmd directly.
- [medium] ensureClaudeCodeSettings et un futur ensureClaudeCodeHooks toucheraient tous les deux .claude/settings.local.json et s'ecraseraient mutuellement. Solution : une seule fonction qui gere permissions ET hooks dans le meme appel read-merge-write.
- [medium] tests/unit/brainclaw-version.test.ts hardcode la version courante (ex: '0.7.0'). Après chaque bump de version dans package.json, mettre à jour getInstalledBrainclawVersion() et les latest_installable_version de test en conséquence — sinon 2 tests échouent silencieusement.
- [medium] vscode-extension/ : le build TypeScript est SÉPARÉ du projet principal (tsconfig CommonJS, outDir ./out). Ne pas inclure dans tsc -p tsconfig.json ni tsconfig.test.json. Pour compiler l'extension : cd vscode-extension && npm install && tsc. L'extension n'est pas testée par la suite de tests principale.
- [medium] vscode-extension/ resolveBrainclawCmd() : si brainclaw n'est pas installé globalement ET pas dans node_modules/.bin/, l'extension retourne silencieusement une page vide sans erreur visible. Vérifier que brainclaw est dispo globalement (brainclaw --version) avant d'utiliser l'extension dans un nouveau workspace.
- [medium] brainclaw install global Windows : npm install -g via cmd /c ou child_process échoue silencieusement (retourne exit 0 mais n'installe pas). Solution validée : powershell -Command "Start-Process cmd -ArgumentList '/c npm install -g <path>' -Verb RunAs -Wait". Vérifier toujours avec brainclaw --version après l'install.
- [medium] brainclaw-version.test.ts : le test 'detects a newer installable build' doit avoir latest_installable_version STRICTEMENT supérieur à la version courante. Après chaque bump majeur/minor, mettre à jour la valeur dans le test (actuellement 1.0.0 pour 0.9.0). Ne pas juste bumper getInstalledBrainclawVersion() sans vérifier ce test.
- [medium] doctor no_curator check: the warn was initially setting hasIssues=true, which made `ok: false` in existing tests that don't register a curator. Fix: no_curator is informational — don't set hasIssues. If you add a doctor check that is truly optional/advisory, use status:'warn' without touching hasIssues.

## Brainclaw — required coordination

Before starting work:
1. Run `brainclaw context` (or `brainclaw context --json`) to load shared memory
2. Run `brainclaw list-claims` — do not edit a file claimed by another agent
3. Create a plan for significant work: `brainclaw plan "<description>"`
4. Claim files you will modify: `brainclaw claim "<description>" --scope <path>`

Before finishing:
1. Run `brainclaw session-end --auto-release` — releases claims and updates plans
2. Or manually: `brainclaw release-claim <id>` and `brainclaw update-plan <id> --status done`
<!-- brainclaw:end -->
