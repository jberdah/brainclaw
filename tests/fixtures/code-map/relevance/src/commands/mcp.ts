export function executeMcpToolCall(): string {
  return 'ok';
}

function __resetConnectionPrincipalForTests(): void {
  // Deliberately internal: this must not become the human-facing brief reason.
}

void __resetConnectionPrincipalForTests;
