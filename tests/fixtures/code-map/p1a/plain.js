// §10: .js extension coverage (javascript grammar).
import { useState } from 'react';

export function makeCounter(start) {
  return { value: start };
}

export const PI = 3.14;

const localOnly = 1;
